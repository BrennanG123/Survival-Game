void gridCheck(){

//1
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 0 -(pixelX*moveSpeed) && mouseX < 96 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 1 + gridX;
 mousey = 7 + gridY;
}


//2
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 96 -(pixelX*moveSpeed) && mouseX < 192 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 2 + gridX;
 mousey = 7 + gridY;
}


//3
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 192 -(pixelX*moveSpeed) && mouseX < 288 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 3 + gridX;
 mousey = 7 + gridY;
}


//4
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 288 -(pixelX*moveSpeed) && mouseX < 384 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 4 + gridX;
 mousey = 7 + gridY;
}


//5
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 384 -(pixelX*moveSpeed) && mouseX < 480 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 5 + gridX;
 mousey = 7 + gridY;
}


//6
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 480 -(pixelX*moveSpeed) && mouseX < 576 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 6 + gridX;
 mousey = 7 + gridY;
}


//7
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 576 -(pixelX*moveSpeed) && mouseX < 672 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 7 + gridX;
 mousey = 7 + gridY;
}


//8
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 672 -(pixelX*moveSpeed) && mouseX < 768 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 8 + gridX;
 mousey = 7 + gridY;
}


//9
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 768 -(pixelX*moveSpeed) && mouseX < 864 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 9 + gridX;
 mousey = 7 + gridY;
}


//10
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 864 -(pixelX*moveSpeed) && mouseX < 960 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 10 + gridX;
 mousey = 7 + gridY;
}


//11
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 960 -(pixelX*moveSpeed) && mouseX < 1056 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 11 + gridX;
 mousey = 7 + gridY;
}


//12
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 0-(pixelY*moveSpeed) && mouseY <95-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 1 + gridY;
}
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 96-(pixelY*moveSpeed) && mouseY <191-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 2 + gridY;
}
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 192-(pixelY*moveSpeed) && mouseY <287-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 3 + gridY;
}
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 288-(pixelY*moveSpeed) && mouseY <383-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 4 + gridY;
}
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 384-(pixelY*moveSpeed) && mouseY <479-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 5 + gridY;
}
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 480-(pixelY*moveSpeed) && mouseY <575-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 6 + gridY;
}
if(mouseX > 1056 -(pixelX*moveSpeed) && mouseX < 1152 -(pixelX*moveSpeed) && mouseY > 576-(pixelY*moveSpeed) && mouseY <671-(pixelY*moveSpeed)){
 mousex = 12 + gridX;
 mousey = 7 + gridY;
}
}
